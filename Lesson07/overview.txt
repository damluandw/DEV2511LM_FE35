1. Javascript
1.1. Biến
    + Khai báo
    + Get/Set
1.2. Cấu trúc điều khiển
    + if else
    + switch case
1.3. Vòng lặp
    + for
    + do while/while
    + foreach, for in,...
1.4. Hàm (function)
    + khai báo: function tên_hàm(tham_số_1,...){ }
    + sử dụng: tên_hàm(truyền_tham_số_1,...)
1.5. Biểu thức: toán học, logic,...
    + toán học:
    + logic: &, ||, !
    + so sánh: ==, === ,...
    + ...
1.6. Object
    + Khai báo
    + Get/Set
1.7. Mảng
    + Khai báo
    + Get/Set
1.8. Hàm xử lý chuỗi/mảng
    + sử dụng: tên_chuỗi/mảng .tên_hàm(truyền_tham_số_1)
1.9. Regular Exception
    + var tên_regx = /cấu_trúc_regx/
    + sử dụng: tên_regx.test(truyền_tham_số)  => true/false
1.10. Date()
    + new Date() - thời gian hiên tại
    + lấy giá trị của thời gian thì sẽ dùng các hàm liên quan đến Date: getYear(), getDay(),...

2. DOM 
- Tương tác với thẻ HTML - DOM element (4 cách cơ bản):
    + thông qua ID: document.getElementById('tên_id')
    + thông qua tên thẻ: document.getElementByTagName('tên_thẻ_HTML')
    + thông tên class: document.getElementByClassName('tên_class')
    + thông qua cú pháp CSS Selector: document.querrySelectAll('#tên_id .tên_class tên_HTML')

- Bắt sự kiện: 
    + DOM Event: onclick, ondbclick, onblur,...  - viết trên thẻ HTML: on_sự_kiện = "hàm_xử_lý()"
    + DOM Listener: vị_trí_thẻ_HTML.addEventListener('sự_kiện', hàm_xử_lý())
- Xử lý HTML/CSS
    + DOM HTML: vị_trí_thẻ_HTML.innerHTML/HTML = giá_trị
    + DOM CSS: vị_trí_thẻ_HTML.style.thuộc_tính_CSS = 
- Navgation
    + parent, sublings, child
3. Jquery 
- Tương tác với thẻ HTML - $('CSS_Selector')
- Bắt sự kiện: 
    + vị_trí_thẻ_HTML.on('sự_kiện', hàm_xử_lý())

- Xử lý HTML/CSS
    + HTML: vị_trí_thẻ_HTML.html()/append()
    + CSS: vị_trí_thẻ_HTML.css('thuộc_tính_CSS') - get
           vị_trí_thẻ_HTML.css('thuộc_tính_CSS', 'giá_trị') - set
- Navgation
    + vị_trí_thẻ_HTML.parent()/sublings()/child()/next()  


=> Bắt sự kiện => xử lý logic (lấy data tại đâu?? - local, giá trị hàm truyền vào, từ khai báo, từ thẻ HTML) => xử lý hiển thị (HTML/CSS)